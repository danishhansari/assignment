const Loader = () => {
  return (
    <div className="h-screen w-full flex items-center justify-center transition-all ">
      <img src="./logo.png" alt="" />
    </div>
  );
};

export default Loader;
